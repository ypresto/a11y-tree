{
  "name": "a11y-tree-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for @a11y-tree/core and @a11y-tree/ai-sdk",
  "author": "ypresto <yuya.presto@gmail.com>",
  "license": "Apache-2.0",
  "homepage": "https://github.com/ypresto/a11y-tree",
  "repository": {
    "type": "git",
    "url": "https://github.com/ypresto/a11y-tree.git"
  },
  "type": "module",
  "scripts": {
    "build": "pnpm --filter \"./packages/*\" run build",
    "test": "pnpm --filter \"./packages/*\" run test",
    "typecheck": "pnpm --filter \"./packages/*\" run typecheck",
    "clean": "pnpm --filter \"./packages/*\" run clean",
    "example:dev": "pnpm --filter a11y-tree-example dev",
    "example:build": "pnpm --filter a11y-tree-example build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/browser": "^2.1.8",
    "playwright": "^1.49.1",
    "tsdown": "^0.22.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
